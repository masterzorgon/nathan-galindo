import { ArticleLayout } from '@/components/ArticleLayout'
import Image from 'next/image'

export const meta = {
  author: 'Nathan Galindo',
  date: '2022-07-07',
  title: 'Rust Tidbits',
  tag: "programming",
  description:
    'A cheat sheet of simple rules and suggestions to facilitate your Rust development journey.',
}

export default (props) => <ArticleLayout meta={meta} {...props} />

You must declare the type of constant variables (`const`):
```rust
const NUMBER: i32 = 3;
fn main() {
    println!("Number {}", NUMBER);
}
```